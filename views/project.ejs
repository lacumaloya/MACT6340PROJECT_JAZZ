<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <meta name="description" content="Jasmine M. Aguilar - Project Details">
  <meta name="author" content="Jasmine M. Aguilar">
  
  <!-- Preconnect to external resources -->
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Kanit:ital,wght@0,100;0,200;0,300;0,400;0,500;0,600;0,700;0,800;0,900;1,100;1,200;1,300;1,400;1,500;1,600;1,700;1,800;1,900&family=Share+Tech+Mono&display=swap" rel="stylesheet">
  
  <!-- Favicon -->
  <link rel="icon" type="image/x-icon" href="/images/menace.ico">
  
  <title><%= project.name %> - Jasmine M. Aguilar</title>
  
  <!-- Stylesheets -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.2.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.1/css/all.min.css">
  <link rel="stylesheet" href="/css/style.css">
</head>

<body class="bg-light d-flex flex-column min-vh-100">
  <%- include('header'); %>

  <main class="flex-grow-1">
    <!-- Project Header -->
    <section class="py-5 fade-in" aria-label="Project header">
      <div class="container">
        <div class="row justify-content-center">
          <div class="col-lg-8 text-center section-header">
            <h1 class="display-4 mb-4"><%= project.name %></h1>
            <p class="lead"><%= project.description %></p>
          </div>
        </div>
      </div>
    </section>

    <!-- Project Details -->
    <section class="py-5 fade-in" aria-label="Project details">
      <div class="container">
        <div class="row justify-content-center">
          <div class="col-lg-8">
            <div class="card shadow-sm">
              <div class="card-body p-4">
                <div class="position-relative mb-4">
                  <img src="<%= project.image %>" 
                       alt="<%= project.name %>" 
                       class="img-fluid rounded">
                  <div class="position-absolute top-0 end-0 m-3">
                    <span class="badge bg-primary"><%= project.editions %></span>
                  </div>
                </div>
                <div class="project-info">
                  <h2 class="h4 mb-3">Project Details</h2>
                  <ul class="list-unstyled">
                    <li class="mb-2"><strong>Name:</strong> <%= project.name %></li>
                    <li class="mb-2"><strong>Description:</strong> <%= project.description %></li>
                    <li class="mb-2"><strong>Editions:</strong> <%= project.editions %></li>
                    <li class="mb-2"><strong>Price:</strong> <%= project.price %></li>
                  </ul>
                </div>
                <div class="d-grid gap-3 mt-4">
                  <button class="btn btn-primary btn-lg" aria-label="Mint NFT">
                    <i class="fas fa-cube me-2"></i>Mint NFT
                  </button>
                  <a href="/projects" class="btn btn-outline-secondary">
                    <i class="fas fa-arrow-left me-2"></i>Back to Projects
                  </a>
                </div>
              </div>
            </div>
          </div>
        </div>
      </div>
    </section>
  </main>

  <%- include('footer'); %>

  <!-- Scripts -->
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.2.3/dist/js/bootstrap.bundle.min.js"></script>
  <script type="module" src="/js/index.js"></script>
</body>
</html>